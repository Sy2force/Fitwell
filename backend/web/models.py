from django.db import models

# Note: This project follows a specific architecture where all data models
# are centralized in the 'api' application to ensure a single source of truth
# for both the REST API and the Web Interface.
#
# Please refer to: backend/api/models.py

